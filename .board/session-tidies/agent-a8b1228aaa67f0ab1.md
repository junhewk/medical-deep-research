# Strategy Tidy: You are designing an implementation plan for the Python project at /home/jk/prog

Thread: @session:agent-a8b1228aaa67f0ab1
Provider: claude
CWD: /home/jk/programming/medical-deep-research
Messages: 122
Signals: 6
Tokens: 13115

## Summary
Started from: You are designing an implementation plan for the Python project at /home/jk/programming/medical-deep-research (medical... Outcome: I have everything needed. Here is the complete, file-by-file implementation pla…: I have everything needed. Here is the complete, file-by-file implementation plan. --- #... Open: Resolve or account for error: exception/crash in output: 130 "get_citations": "searching", 131 "get_studies": "ranking", 132 "screen_studies": "screening",...

## Initial Prompt
You are designing an implementation plan for the Python project at /home/jk/programming/medical-deep-research (medical deep research agent). Do NOT write code — produce a precise, file-and-line-level implementation plan. Read the relevant files first.

## Goal
Expand literature search from ~10 articles/source to up to 25/source, and add an article-organization strategy so the agent can cope with the larger candidate pool. The user has chosen:
1. **Max per source = 25** (raise the hard cap).
2. **Triage strategy = Tiered / paged**: `get_studies` returns the top tier first (deterministically pre-ranked, grouped by evidence level I→V); the agent can page/expand by evidence level or source on demand (likely via a NEW tool that reads the already-scored pool without resetting state).
3. **Screening = explicit includes only (whitelist)**: change `tool_screen_studies` so only studies the agent explicitly includes survive; everything else is dropped (recorded as excluded). Today, unlisted indices default to INCLUDED — this must flip.
4. **Raise the report cap**: more studies should reach the final synthesized report (today capped at top 12).

## Key facts already established (verify, don't re-discover from scratch)
- Constants: `src/medical_deep_research/runtime.py:74-75` — `DEFAULT_SEARCH_RESULTS_PER_SOURCE = 8`, `MAX_AGENT_SEARCH_RESULTS_PER_SOURCE = 20`. The runtime clamps requests to [1,20] at `runtime.py:2956`.
- Prompt soft-cap: `src/medical_deep_research/agentic_tools.py:2174` says "do not request max_results above 10" — this is the real reason the agent self-limits to 10. The system prompt also has a 13-step workflow (~lines 2150-2189) and a "Search Discipline" section (~2169-2175).
- `tool_get_studies` (`agentic_tools.py:476`) flattens all search results → `deduplicate_studies` → `score_and_rank_results` (scoring.py:283, computes composite_score, sorts desc, assigns reference_number) → returns the ENTIRE pool to the agent with 500-char abstracts. It also sets `bridge.screening = None` and rebuilds `bridge._pre_scored` each call.
- `tool_screen_studies` (`agentic_tools.py:589`): unlisted indices currently default to INCLUDED.
- `tool_finalize_ranking` (`agentic_tools.py:509`): auto-appends any un-ranked studies at the end.
- Report caps: `agentic_tools.py:285` (`self.ranked_studies[:12]` artifact) and `agentic_tools.py:720` (`bridge.ranked_studies[:12]` synthesis input). Deterministic fallback `research/reporting.py:40` (`ranked_studies[:8]`).
- Studies carry `evidence_level`, `composite_score`, `sources`, `reference_number` fields (see scoring.py ScoredStudy / research/models.py).

## What I need from you
Produce a detailed plan covering:

1. **Tool registration across ALL provider runtimes.** This is the critical unknown. Search `runtime.py` for how tools are registered for each provider path: the LangChain `_make_search`/`@lc_tool` path (~2951-2963, and where get_studies/screen_studies/finalize_ranking are registered as lc_tools), the dict/schema-based `_wrap` path (~2189-2199 area), and any Anthropic/OpenAI/local/MCP tool lists. Find EVERY place a tool like `get_studies` or `screen_studies` is declared, so a NEW `browse_studies`/`get_more_studies` tool gets wired into every provider. List exact functions/line ranges. Also find `TOOL_DESCRIPTIONS` (agentic_tools.py ~2405-2433) and `_SEARCH_SOURCES`.

2. **Tiered get_studies + new paging tool design.** Specify: what `get_studies` should now return (top tier sorted/grouped by evidence level then composite score, plus facet metadata: total count, counts per evidence level, counts per source, "N of M shown"); the signature + behavior of the new tool that reads `bridge._pre_scored` WITHOUT re-scoring or resetting `bridge.screening` (params: page/offset, evidence_level filter, source filter); a tier/page-size constant. Make sure paging never wipes screening state.

3. **Whitelist screening.** Exact change to `tool_screen_studies` so kept = explicitly-included only; everything else recorded as excluded (preserve ability to record reasons for notable exclusions + a bulk "not selected" category for the Methods section). Note the prompt step that must change.

4. **Numbers + constants.** Recommend concrete values: cap 20→25; whether to raise DEFAULT (8→?) and where that default propagates (runtime tool schema default at ~2196, `tool_search` default at agentic_tools.py:447, lc_tool default at 2953, mcp/servers.py defaults); a new `MAX_REPORT_STUDIES` constant (~?) replacing the `[:12]`/`[:8]` slices; a `STUDY_PAGE_SIZE` constant. Update the clamp at runtime.py:2956.

5. **System prompt updates.** Which lines in the 13-step workflow + Search Discipline to change: remove/raise the "≤10" guidance (line 2174), teach the agent to search wider, expect a pre-ranked tier, page for more by level/source, screen as an explicit include-list, and order by evidence level I→V.

6. **Tests that will break.** Grep the tests directory (likely `tests/`) for anything asserting screen_studies default-include behavior, get_studies returning the full pool, the 12/8 report caps, or the max_results=20 clamp. List the test files/functions that need updating and what the new assertions should be.

Return a concrete, ordered, file-by-file implementation plan with exact symbols and line anchors, the recommended constant values, the new tool's signature and return shape, and the list of tests to update. Be specific enough that another engineer could execute it without re-exploring.

## Decisions
(none detected)

## Plans
(none detected)

## Conclusions
- I have everything needed. Here is the complete, file-by-file implementation pla…: I have everything needed. Here is the complete, file-by-file implementation plan. --- # Implementation Plan: Wider Search (25/source) + Tiered Triage + Whitelist Screening + Higher Report Cap ## 0. Architecture summary (what I verified) There are **three in-process agentic tool registration paths**, all wrapping the same shared `tool_*` coroutines in `agentic_tools.py`, plus one **standalone FastMCP** surface and one **non-agentic "native loop"**: | Path | Builder fn (runtime.py) | Wrap style | get_studies / screen_studies anchors | Result post-processing | |---|---|---|---|---| | OpenAI Agen…

## Results
- Error: exception/crash in output: 2130 """Build OpenAI ``FunctionTool`` instances wrapping shared tool functions.""" 2131 from agents import FunctionTool 2132 2133 tools: list[Any] = [] 2134 2135 def _wrap(name: str, schema: dict[str, Any], coro_factory: Any) -> Any: # noqa: ANN401 2136 """Create a FunctionTool that emits bridge events around the call.""" 2137 2138 async def _invoke(_ctx: Any, args_json: str) -> str: 2139 args = json.loads(args_json) if args_json else {} 2140 await bridge.on_tool_start(name, args) 2141 try: 2142 result = await coro_factory(args) 2143 except Exception as exc: 2144 await bridge.on_tool_end(name…
- Error: exception/crash in output: 88:def _env_float(name: str, default: float) -> float: 99:def _env_int(name: str, default: int) -> int: 110:def _ev( 211:def _format_exception(exc: Exception | None) -> str | None: 220:def _trim_diagnostic_text(text: str | None, *, max_chars: int = 2000) -> str | None: 231:def _exception_diagnostics(exc: Exception | None) -> dict[str, Any]: 246:def _agentic_failure_diagnostics(bridge: AgenticEventBridge) -> dict[str, Any]: 275:def _source_tool_name(source: str) -> str: 279:def _provider_api_env(provider: str, api_keys: dict[str, str]) -> dict[str, str]: 302:def _search_api_env(api_keys: dict[…
- Error: exception/crash in output: 2580 } 2581 2582 2583 class ClaudeSDKAnthropicRuntime(NativeSDKRuntime): 2584 provider = "anthropic" 2585 runtime_name = "Anthropic Agent SDK" 2586 sdk_module = "claude_agent_sdk" 2587 runtime_engine = "claude_sdk_legacy" 2588 planner_name = "Claude Planner" 2589 search_agent_name = "Claude Search Agent" 2590 synthesis_agent_name = "Claude Synthesis Agent" 2591 verifier_name = "Claude Verification Agent" 2592 native_agent_name = "Claude MCP Research Agent" 2593 2594 async def stream_run(self, request: RunRequest) -> AsyncIterator[RuntimeEventPayload]: 2595 if self._should_fallback(request): 2…
- Error: exception/crash in output: 1536 async def _apply_screening( 1537 self, 1538 request: RunRequest, 1539 *, 1540 plan: QueryPlan, 1541 ranked: list[ScoredStudy], 1542 ) -> tuple[list[ScoredStudy], dict[str, Any], str | None]: 1543 """Run the screening checkpoint; on failure, include all studies. 1544 1545 Returns the (renumbered) included studies, a screening summary, and an 1546 optional note for the rewind decision. 1547 """ 1548 title_by_ref = {s.reference_number: s.title for s in ranked} 1549 try: 1550 decision = await asyncio.wait_for( 1551 self._request_screening(request, plan=plan, pre_ranked=ranked), 1552 timeout=…
- 240 "verify_studies", 241 "synthesize_report", 242 "plan_search", 243 "fetch_fulltext", 244 "await_user_pdfs", 245 ): 246 self._intermediate[bare] = response 247 extra: dict[str,...
- 1 from __future__ import annotations 2 3 import argparse 4 import json 5 import os 6 from typing import Any 7 8 from mcp.server.fastmcp import FastMCP 9 from sqlmodel import...
- 1300 tracker: ProgressTracker, 1301 ) -> _IterationResult: 1302 events: list[RuntimeEventPayload] = [] 1303 provider_results: list[SearchProviderResult] = [] 1304 notes:...
- 780 yield _ev( 781 tracker, 782 EventType.ARTIFACT_CREATED, 783 "ranking", 784 "Saved ranked evidence artifact", 785 artifact_type=ArtifactType.RANKED_RESULTS, 786...

## Changes
- Read: /home/jk/programming/medical-deep-research/src/medical_deep_research/research/reporting.py
- Command: grep -n "evidence_level\|class ScoredStudy\|class EvidenceStudy\|reference_number\|composite_score\|sources\b\|class QueryPlan" src/medical_deep_research/research/models.py |...
- Command: grep -rn "screen_studies\|tool_screen_studies\|get_studies\|tool_get_studies\|finalize_ranking\|_pre_scored\|included_indices\|excluded_indices\|default.*includ\|max_results\|MAX_...
- Read: /home/jk/programming/medical-deep-research/tests/test_local_runtime.py
- Command: grep -rn "def fake_search_source\|def make_request\|fake_search_source\s*=" tests/ | head; echo "---FAKE STUDIES---"; grep -rn...
- Read: /home/jk/programming/medical-deep-research/tests/test_native_loop_stages.py
- Command: grep -n "_apply_screening\|\[:12\]\|\[:8\]\|\[:10\]\|\[:20\]\|ranked\[:\|ranked_studies\[:\|top_studies\|included_reference\|default.*includ\|not.*explicit"...
- Read: /home/jk/programming/medical-deep-research/src/medical_deep_research/runtime.py
- Command: grep -rn "screen\|included_indices\|excluded\|get_studies\|\"total\"\|\['total'\]\|browse_studies\|page\|tier\|MAX_REPORT\|STUDY_PAGE\|len(ranked)\|len(studies)"...
- Command: grep -rn "EVIDENCE_LEVEL\|level_rank\|_level_order\|Level I.*Level V\|def get_evidence_level_score\|evidence_level_order\|sort.*evidence_level"...
- Command: grep -rln "agentic_system_prompt\|max_results above\|Search Discipline\|do not request\|_build_langchain_tools\|_build_openai_tools\|_build_anthropic_mcp" tests/; echo...
- Read: /home/jk/programming/medical-deep-research/src/medical_deep_research/agentic_tools.py

## Open Items
- Resolve or account for error: exception/crash in output: 130 "get_citations": "searching", 131 "get_studies": "ranking", 132 "screen_studies": "screening", 133 "finalize_ranking": "ranking", 134 "appraise_evidence": "appraising", 135 "verify_studies": "verifying", 136 "synthesize_report": "synthesizing", 137 "fetch_fulltext": "fulltext", 138 "await_user_pdfs": "fulltext", 139 "parse_pdf": "fulltext", 140 } 141 142 # Bookkeeping tools follow whatever phase the run is currently in instead of 143 # flipping the trace back to "planning" on every call. 144 _CURRENT_PHASE_TOOLS = {"write_todos", "update_progress"} 145 146 147 class AgenticEventBridge: 14…
- Resolve or account for error: exception/crash in output: 2130 """Build OpenAI ``FunctionTool`` instances wrapping shared tool functions.""" 2131 from agents import FunctionTool 2132 2133 tools: list[Any] = [] 2134 2135 def _wrap(name: str, schema: dict[str, Any], coro_factory: Any) -> Any: # noqa: ANN401 2136 """Create a FunctionTool that emits bridge events around the call.""" 2137 2138 async def _invoke(_ctx: Any, args_json: str) -> str: 2139 args = json.loads(args_json) if args_json else {} 2140 await bridge.on_tool_start(name, args) 2141 try: 2142 result = await coro_factory(args) 2143 except Exception as exc: 2144 await bridge.on_tool_end(name…
- Resolve or account for error: exception/crash in output: 88:def _env_float(name: str, default: float) -> float: 99:def _env_int(name: str, default: int) -> int: 110:def _ev( 211:def _format_exception(exc: Exception | None) -> str | None: 220:def _trim_diagnostic_text(text: str | None, *, max_chars: int = 2000) -> str | None: 231:def _exception_diagnostics(exc: Exception | None) -> dict[str, Any]: 246:def _agentic_failure_diagnostics(bridge: AgenticEventBridge) -> dict[str, Any]: 275:def _source_tool_name(source: str) -> str: 279:def _provider_api_env(provider: str, api_keys: dict[str, str]) -> dict[str, str]: 302:def _search_api_env(api_keys: dict[…
- Resolve or account for error: exception/crash in output: 2580 } 2581 2582 2583 class ClaudeSDKAnthropicRuntime(NativeSDKRuntime): 2584 provider = "anthropic" 2585 runtime_name = "Anthropic Agent SDK" 2586 sdk_module = "claude_agent_sdk" 2587 runtime_engine = "claude_sdk_legacy" 2588 planner_name = "Claude Planner" 2589 search_agent_name = "Claude Search Agent" 2590 synthesis_agent_name = "Claude Synthesis Agent" 2591 verifier_name = "Claude Verification Agent" 2592 native_agent_name = "Claude MCP Research Agent" 2593 2594 async def stream_run(self, request: RunRequest) -> AsyncIterator[RuntimeEventPayload]: 2595 if self._should_fallback(request): 2…
- Resolve or account for error: exception/crash in output: 1536 async def _apply_screening( 1537 self, 1538 request: RunRequest, 1539 *, 1540 plan: QueryPlan, 1541 ranked: list[ScoredStudy], 1542 ) -> tuple[list[ScoredStudy], dict[str, Any], str | None]: 1543 """Run the screening checkpoint; on failure, include all studies. 1544 1545 Returns the (renumbered) included studies, a screening summary, and an 1546 optional note for the rewind decision. 1547 """ 1548 title_by_ref = {s.reference_number: s.title for s in ranked} 1549 try: 1550 decision = await asyncio.wait_for( 1551 self._request_screening(request, plan=plan, pre_ranked=ranked), 1552 timeout=…

## Workflow
- [error] exception/crash in output: 130 "get_citations": "searching", 131 "get_studies": "ranking", 132 "screen_studies": "screening", 133 "finalize_ranking": "ranking", 134 "appraise_evidence": "appraising", 135 "verify_studies": "verifying", 136 "synthesize_report": "synthesizing", 137 "fetch_fulltext": "fulltext", 138 "await_user_pdfs": "fulltext", 139 "parse_pdf": "fulltext", 140 } 141 142 # Bookkeeping tools follow whatever phase the run is currently in instead of 143 # flipping the trace back to "planning" on every call. 144 _CURRENT_PHASE_TOOLS = {"write_todos", "update_progress"} 145 146 147 class AgenticEventBridge: 14…
- [error] exception/crash in output: 2130 """Build OpenAI ``FunctionTool`` instances wrapping shared tool functions.""" 2131 from agents import FunctionTool 2132 2133 tools: list[Any] = [] 2134 2135 def _wrap(name: str, schema: dict[str, Any], coro_factory: Any) -> Any: # noqa: ANN401 2136 """Create a FunctionTool that emits bridge events around the call.""" 2137 2138 async def _invoke(_ctx: Any, args_json: str) -> str: 2139 args = json.loads(args_json) if args_json else {} 2140 await bridge.on_tool_start(name, args) 2141 try: 2142 result = await coro_factory(args) 2143 except Exception as exc: 2144 await bridge.on_tool_end(name…
- [error] exception/crash in output: 88:def _env_float(name: str, default: float) -> float: 99:def _env_int(name: str, default: int) -> int: 110:def _ev( 211:def _format_exception(exc: Exception | None) -> str | None: 220:def _trim_diagnostic_text(text: str | None, *, max_chars: int = 2000) -> str | None: 231:def _exception_diagnostics(exc: Exception | None) -> dict[str, Any]: 246:def _agentic_failure_diagnostics(bridge: AgenticEventBridge) -> dict[str, Any]: 275:def _source_tool_name(source: str) -> str: 279:def _provider_api_env(provider: str, api_keys: dict[str, str]) -> dict[str, str]: 302:def _search_api_env(api_keys: dict[…
- [error] exception/crash in output: 2580 } 2581 2582 2583 class ClaudeSDKAnthropicRuntime(NativeSDKRuntime): 2584 provider = "anthropic" 2585 runtime_name = "Anthropic Agent SDK" 2586 sdk_module = "claude_agent_sdk" 2587 runtime_engine = "claude_sdk_legacy" 2588 planner_name = "Claude Planner" 2589 search_agent_name = "Claude Search Agent" 2590 synthesis_agent_name = "Claude Synthesis Agent" 2591 verifier_name = "Claude Verification Agent" 2592 native_agent_name = "Claude MCP Research Agent" 2593 2594 async def stream_run(self, request: RunRequest) -> AsyncIterator[RuntimeEventPayload]: 2595 if self._should_fallback(request): 2…
- [error] exception/crash in output: 1536 async def _apply_screening( 1537 self, 1538 request: RunRequest, 1539 *, 1540 plan: QueryPlan, 1541 ranked: list[ScoredStudy], 1542 ) -> tuple[list[ScoredStudy], dict[str, Any], str | None]: 1543 """Run the screening checkpoint; on failure, include all studies. 1544 1545 Returns the (renumbered) included studies, a screening summary, and an 1546 optional note for the rewind decision. 1547 """ 1548 title_by_ref = {s.reference_number: s.title for s in ranked} 1549 try: 1550 decision = await asyncio.wait_for( 1551 self._request_screening(request, plan=plan, pre_ranked=ranked), 1552 timeout=…
- [conclusion] I have everything needed. Here is the complete, file-by-file implementation pla…: I have everything needed. Here is the complete, file-by-file implementation plan. --- # Implementation Plan: Wider Search (25/source) + Tiered Triage + Whitelist Screening + Higher Report Cap ## 0. Architecture summary (what I verified) There are **three in-process agentic tool registration paths**, all wrapping the same shared `tool_*` coroutines in `agentic_tools.py`, plus one **standalone FastMCP** surface and one **non-agentic "native loop"**: | Path | Builder fn (runtime.py) | Wrap style | get_studies / screen_studies anchors | Result post-processing | |---|---|---|---|---| | OpenAI Agen…